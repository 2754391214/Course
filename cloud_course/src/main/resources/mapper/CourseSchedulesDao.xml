<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyw.cloudCourse.mapper.CourseSchedulesDao">
    <select id="checkTimeConflictInDB" resultType="java.lang.Integer">
        SELECT COUNT(1)
        FROM course_schedules cs1
        INNER JOIN course_schedules cs2 ON cs1.day_of_week = cs2.day_of_week
        WHERE cs1.course_id = #{courseId}
        AND cs2.course_id IN
        <foreach collection="enrolledCourseIds" item="courseId" open="(" close=")" separator=",">
            #{courseId}
        </foreach>
        AND cs1.start_time <![CDATA[ < ]]> cs2.end_time
        AND cs1.end_time  <![CDATA[ > ]]>  cs2.start_time
    </select>
</mapper>
