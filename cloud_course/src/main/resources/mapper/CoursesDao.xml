<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyw.cloudCourse.mapper.CoursesDao">

    <select id="selectPopularCourses" resultType="com.lyw.cloudCourse.vo.CoursesVo">
        SELECT c.*, cs.hot_score
        FROM courses c
                 LEFT JOIN course_stats cs ON c.id = cs.course_id
        <where>
            c.status = 'PUBLISHED'
            AND (c.semester = #{semester} OR #{semester} IS NULL)
            <if test="departmentId != null">
                AND c.department_id=#{departmentId}
            </if>
        </where>
        ORDER BY cs.hot_score DESC
            LIMIT #{limit};
    </select>
    <select id="selectCategoryCourses" resultType="com.lyw.cloudCourse.vo.CoursesVo">
        SELECT c.*
        FROM courses c
                 LEFT JOIN course_category_relations ccr ON c.id = ccr.course_id
                 LEFT JOIN course_categories cc ON cc.id = ccr.category_id
            ${ew.customSqlSegment}
    </select>
</mapper>