<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyw.cloudMember.mapper.TeacherProfileDao">


    <update id="unbindUser">
        UPDATE teacher_profile SET user_id = NULL WHERE user_id = #{userId} AND teacher_id = #{teacherId}
    </update>
</mapper>
