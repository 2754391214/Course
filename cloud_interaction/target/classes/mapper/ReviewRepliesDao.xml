<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyw.cloudInteraction.mapper.ReviewRepliesDao">


    <update id="incrementLikeCount">
        UPDATE review_replies SET like_count = like_count + 1 WHERE id = #{replyId}
    </update>
    <update id="decrementLikeCount">
        UPDATE review_replies SET like_count = GREATEST(like_count - 1, 0) WHERE id = #{replyId}
    </update>
    <update id="updateLikeCount">
        UPDATE review_replies SET like_count = #{count} WHERE id = #{replyId}
    </update>
</mapper>
