<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyw.cloudChoose.mapper.LocalMessageDao">

    <insert id="batchInsertMessages">
        INSERT INTO local_message (
        business_key,
        message_type,
        exchange,
        routing_key,
        message_body,
        status,
        retry_count,
        max_retry_count,
        next_retry_time,
        error_message,
        cru,
        crd,
        luu,
        lud
        ) VALUES
        <foreach collection="messages" item="msg" separator=",">
            (
            #{msg.businessKey},
            #{msg.messageType},
            #{msg.exchange},
            #{msg.routingKey},
            #{msg.messageBody},
            #{msg.status},
            #{msg.retryCount},
            #{msg.maxRetryCount},
            #{msg.nextRetryTime},
            #{msg.errorMessage},
            #{msg.cru},
            #{msg.crd},
            #{msg.luu},
            #{msg.lud}
            )
        </foreach>
    </insert>

</mapper>
